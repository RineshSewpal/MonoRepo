{
    "name": "my-monorepo",
    "private": true,
    "scripts": {
        "test": "pnpm -r test",
        "typecheck": "tsc --noEmit -p tsconfig.json",
        "build": "pnpm -r build"
    },
    "devDependencies": {
        "@types/better-sqlite3": "^7.6.13",
        "supertest": "^7.2.2",
        "typescript": "^5.9.3"
    },
    "dependencies": {
        "better-sqlite3": "^12.6.2"
    }
}